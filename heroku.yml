setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: ORDERS_DATABASE
    - plan: heroku-postgresql:essential-0
      as: ADMINS_DATABASE
  config:
    ASPNETCORE_ENVIRONMENT: Production

build:
  docker:
    web: Backend/Dockerfile
